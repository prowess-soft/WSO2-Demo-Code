openapi: 3.0.1
info:
  title: DeleteStoreOrder
  description: API Definition of DeleteStoreOrder
  version: 1.0.0
servers:
- url: /StoreOrder
paths:
  /order/{orderId}:
    delete:
      parameters:
      - name: orderId
        in: path
        required: true
        style: simple
        explode: false
        schema:
          type: string
      responses:
        default:
          description: Default response
